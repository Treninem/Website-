name: work_portal
publish_to: "none"
description: Private employee work portal mobile client for Android and iOS.
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
