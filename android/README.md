# Android application

Native Android client for Website. The application will use the same backend API as the web version, so users, roles and permissions remain centralized.

Initial plan:
- secure login and registration by one-time invitation key;
- role-aware interface;
- HTTPS-only production API;
- later: offline drafts/queue if needed.
